# i18n (다국어) 지원 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 안드로이드 기기 언어 설정(또는 앱 내 수동 선택)에 따라 UI 전체를 한국어/영어로 전환하는 i18n 시스템 구현

**Architecture:** `js/locales/ko.js` + `js/locales/en.js`에 모든 문자열 저장. `js/utils/i18n.js`의 `t(key, vars)` 함수로 현재 언어 문자열 반환. `localechange` 이벤트로 앱 전체 재렌더. 설정 페이지에서 수동 변경 가능.

**Tech Stack:** Vanilla JS ES Modules, IndexedDB (설정 저장), Capacitor Android

---

## 파일 구조

**신규 생성:**
- `js/locales/ko.js` — 한국어 문자열
- `js/locales/en.js` — 영어 문자열

**수정:**
- `js/utils/i18n.js` — 완전 재작성 (t, initLocale, setLocale, getLocale, getMessages)
- `js/utils/date.js` — locale-aware 날짜 포맷
- `js/app.js` — initLocale() 호출 + localechange 리스너
- `js/router.js` — 에러 메시지 t() 사용
- `js/components/modal.js` — 기본 버튼 텍스트 t()
- `js/components/bottom-nav.js` — 네비 라벨 t() + localechange 재렌더
- `js/components/sidebar.js` — 네비 라벨 t() + localechange 재렌더
- `js/pages/login.js` — 하드코딩 한국어 → t()
- `js/pages/dashboard.js` — 하드코딩 한국어 → t()
- `js/pages/groups.js` — 하드코딩 한국어 → t()
- `js/pages/group-detail.js` — 하드코딩 한국어 → t()
- `js/pages/attendance.js` — 하드코딩 한국어 → t()
- `js/pages/statistics.js` — 하드코딩 한국어 → t()
- `js/pages/calendar.js` — 하드코딩 한국어 → t()
- `js/pages/calendar-all.js` — 하드코딩 한국어 → t()
- `js/pages/settings.js` — 하드코딩 한국어 → t() + 언어 선택 UI
- `js/pages/export.js` — 하드코딩 한국어 → t()
- `js/pages/premium.js` — 하드코딩 한국어 → t()
- `index.html` — lang 속성 + 메타 태그

---

## Task 1: 로케일 파일 생성 (ko.js + en.js)

**Files:**
- Create: `js/locales/ko.js`
- Create: `js/locales/en.js`

- [ ] **Step 1: `js/locales/ko.js` 생성**

```js
/**
 * Korean locale strings
 */
export default {
  nav: {
    home: '홈',
    groups: '그룹',
    calendar: '달력',
    stats: '통계',
    settings: '설정',
    dashboard: '대시보드',
    groupMgmt: '그룹 관리',
    myGroups: '내 그룹',
    addGroup: '+ 그룹 추가',
    noGroupsShort: '그룹이 없습니다',
    mainMenu: '메인 메뉴',
    bottomNav: '하단 메뉴',
  },
  status: {
    present: '출석',
    absent:  '결석',
    late:    '지각',
    early:   '조퇴',
    none:    '미입력',
  },
  date: {
    today:     '오늘',
    yesterday: '어제',
    daysShort: ['일', '월', '화', '수', '목', '금', '토'],
    months:    ['1월', '2월', '3월', '4월', '5월', '6월', '7월', '8월', '9월', '10월', '11월', '12월'],
    full:      '{year}년 {month}월 {day}일 ({dow})',
    short:     '{month}/{day}({dow})',
    monthDay:  '{month}월 {day}일 ({dow})',
    yearMonth: '{year}년 {month}월',
  },
  common: {
    back:          '뒤로',
    save:          '저장',
    cancel:        '취소',
    confirm:       '확인',
    delete:        '삭제',
    edit:          '수정',
    add:           '추가',
    close:         '닫기',
    loading:       '로딩 중...',
    optional:      '(선택)',
    required:      '*',
    noData:        '데이터가 없습니다',
    groupNotFound: '그룹을 찾을 수 없습니다',
  },
  dashboard: {
    title:            '대시보드',
    totalGroups:      '전체 그룹',
    totalStudents:    '전체 학생',
    todayPresent:     '오늘 출석',
    todayAbsent:      '오늘 결석',
    todayStatus:      '오늘 그룹별 현황',
    manageAll:        '전체 관리 →',
    contractExpiry:   '⚠ 계약 종료 예정',
    criteriaSettings: '기준 설정 →',
    periodBadge:      '기간제',
    countBadge:       '횟수제',
    endsToday:        '오늘 종료',
    daysLeft:         '{{n}}일 남음',
    exhausted:        '소진',
    remaining:        '잔여 {{n}}회',
    manage:           '관리 →',
    attendCheck:      '출석 체크',
    studentCount:     '{{n}}명',
    present:          '출석',
    absent:           '결석',
    notEntered:       '미입력',
    welcome:          '환영합니다!',
    welcomeDesc:      '그룹을 만들어 출석을 시작해 보세요.',
    firstGroup:       '+ 첫 그룹 만들기',
  },
  groups: {
    title:               '그룹 관리',
    addGroup:            '+ 새 그룹',
    noGroups:            '그룹이 없습니다',
    noGroupsDesc:        '새 그룹을 만들어 학생들을 관리해 보세요.',
    addGroupBtn:         '+ 새 그룹 만들기',
    addGroupCard:        '새 그룹 추가',
    newGroupTitle:       '새 그룹',
    editGroupTitle:      '그룹 수정',
    groupName:           '그룹 이름',
    groupDesc:           '설명',
    color:               '색상',
    create:              '만들기',
    studentCountLoading: '학생 수 로딩중...',
    studentCount:        '학생 {{n}}명',
  },
  groupDetail: {
    memberCount:    '구성원 {{n}}명',
    todayStatus:    '{{monthDay}} 출석 현황',
    attendCheck:    '오늘 출석 체크',
    addMember:      '+ 구성원 추가',
    memberList:     '구성원 목록',
    addShort:       '+ 추가',
    noMembers:      '구성원이 없습니다',
    noMembersDesc:  '구성원을 추가해 출석을 관리해 보세요.',
    addMemberEmpty: '+ 구성원 추가',
    searchNoResult: '"{{query}}" 검색 결과가 없습니다.',
    backToGroups:   '그룹 목록으로',
    attendLink:     '📋 출석 체크',
    calendarLink:   '📅 달력',
    statsLink:      '📊 통계',
    genderMale:     '남',
    genderFemale:   '여',
    genderOther:    '기타',
    colNumber:      '번호',
    colName:        '이름',
    colAge:         '나이',
    colGender:      '성별',
    colPhone:       '연락처',
    colDays:        '출석 요일',
    colTime:        '시간',
    colReg:         '등록일',
    colContract:    '계약',
    colToday:       '오늘',
    colManage:      '관리',
    addError:       '오류: {{msg}}',
  },
  attendance: {
    title:       '출석 체크',
    prevDay:     '이전날',
    nextDay:     '다음날',
    pickDate:    '날짜 선택',
    allStudents: '전체:',
    markAll:     '모두 {{status}}',
    markSched:   '예정 {{status}}',
    scheduled:   '오늘 출석 예정',
    unscheduled: '출석 비예정',
    count:       '{{n}}명',
    clearAll:    '초기화',
    noStudents:  '학생이 없습니다',
    addStudents: '학생 추가하기',
  },
  statistics: {
    title:        '통계',
    calculating:  '통계를 계산하는 중...',
    monthlyRate:  '이달 출석률',
    studentCount: '학생 수',
    classDays:    '수업일',
    avgAbsent:    '평균 결석',
    dailyChart:   '일별 출석 현황',
    studentTable: '학생별 출석 현황',
    colNumber:    '번호',
    colName:      '이름',
    colPresent:   '출석',
    colAbsent:    '결석',
    colLate:      '지각',
    colEarly:     '조퇴',
    colRate:      '출석률',
    noData:       '데이터가 없습니다',
  },
  calendar: {
    title:      '달력',
    allTitle:   '📅 달력',
    loading:    '달력을 불러오는 중...',
  },
  export: {
    title:     '내보내기',
    dateRange: '기간 선택',
    startDate: '시작일',
    endDate:   '종료일',
    csvBtn:    '📄 CSV 내보내기',
    pdfBtn:    '📑 PDF 내보내기',
  },
  settings: {
    title:            '설정',
    account:          '계정',
    language:         '언어',
    systemLanguage:   '시스템 언어 사용',
    localMode:        '로컬 모드',
    localModeDesc:    '데이터가 이 기기에만 저장됩니다',
    loginGoogleDesc:  'Google 로그인 시 현재 데이터를 클라우드에 백업하고 모든 기기에서 동기화할 수 있습니다.',
    loginGoogle:      'Google로 로그인',
    logout:           '로그아웃',
    logoutConfirm:    '로그아웃하시겠습니까?',
    noName:           '이름 없음',
    migrationTitle:   '기존 데이터 마이그레이션',
    migrationDesc:    '이전 버전(로컬 저장)의 데이터가 감지되었습니다. 아래 버튼을 눌러 클라우드로 가져오세요.',
    migrateBtn:       '기기 데이터 가져오기',
    migratingBtn:     '가져오는 중...',
    migrateModalTitle: '기기 데이터 가져오기',
    migrateModalMsg:  '기기에 저장된 기존 데이터를 클라우드로 가져옵니다.\n현재 클라우드 데이터는 모두 교체됩니다.\n계속하시겠습니까?',
    migrateModalBtn:  '가져오기',
    contractAlert:    '계약 종료 예정 알림 기준',
    periodType:       '기간제',
    periodTypeDesc:   '종료일 N일 이내인 학생을 홈에 표시',
    daysUnit:         '일 이내',
    countType:        '횟수제',
    countTypeDesc:    '잔여 횟수가 N회 이하인 학생을 홈에 표시',
    countUnit:        '회 이하',
    saveBtn:          '저장',
    appInfo:          '앱 정보',
    version:          '버전',
    developer:        '개발자',
  },
  login: {
    subtitle:    '학생 출결을 쉽고 빠르게 관리하세요',
    googleSignIn: 'Google로 로그인',
    signingIn:   '로그인 중...',
    or:          '또는',
    guestMode:   '로그인 없이 사용하기',
    termsText:   '계속 진행하면 출석부의 {{terms}} 및 {{privacy}}에 동의하게 됩니다.',
    terms:       '이용약관',
    privacy:     '개인정보처리방침',
  },
  premium: {
    title:        '프리미엄',
    upgradeTitle: '프리미엄 업그레이드',
    alreadyTitle: '이미 프리미엄 사용자입니다!',
    alreadyDesc:  '모든 기능을 제한 없이 사용할 수 있습니다.',
    goHome:       '홈으로',
    heroTitle:    '프리미엄으로 업그레이드',
    heroDesc:     '모든 기능을 제한 없이 사용하세요',
    lifetime:     '평생 이용권',
    f1Icon:  '👥', f1Title: '무제한 그룹/학생',    f1Desc: '그룹과 학생 수에 제한 없이 사용',
    f2Icon:  '📊', f2Title: '고급 통계 차트',      f2Desc: '일별/월별 출석 현황 차트 제공',
    f3Icon:  '📄', f3Title: 'CSV/PDF 내보내기',    f3Desc: '출석 데이터를 엑셀, PDF로 내보내기',
    f4Icon:  '🚫', f4Title: '광고 없음',           f4Desc: '광고 없이 쾌적한 환경',
  },
  messages: {
    groupCreated:           '그룹이 생성되었습니다.',
    groupUpdated:           '그룹이 수정되었습니다.',
    groupDeleted:           '그룹이 삭제되었습니다.',
    studentCreated:         '학생이 추가되었습니다.',
    studentUpdated:         '학생 정보가 수정되었습니다.',
    studentDeleted:         '학생이 삭제되었습니다.',
    attendanceSaved:        '출석이 저장되었습니다.',
    exportDone:             '내보내기 완료',
    backupSaved:            '백업 파일이 저장되었습니다.',
    backupLoaded:           '백업을 불러왔습니다.',
    premiumActivated:       '프리미엄이 활성화되었습니다! 🎉',
    loadFailed:             '데이터를 불러오는 데 실패했습니다.',
    saveFailed:             '저장에 실패했습니다.',
    nameRequired:           '이름을 입력해 주세요.',
    invalidFile:            '올바르지 않은 파일 형식입니다.',
    freeGroupLimit:         '무료 플랜은 그룹 1개까지 사용할 수 있습니다.\n프리미엄으로 업그레이드하면 무제한으로 사용할 수 있습니다.',
    freeStudentLimit:       '무료 플랜은 그룹당 학생 20명까지 등록할 수 있습니다.\n프리미엄으로 업그레이드하면 무제한으로 사용할 수 있습니다.',
    premiumRequired:        '이 기능은 프리미엄 플랜에서만 사용할 수 있습니다.',
    deleteGroupConfirm:     '"{{name}}" 그룹을 삭제하면 모든 학생과 출석 기록도 함께 삭제됩니다. 계속하시겠습니까?',
    deleteStudentConfirm:   '"{{name}}" 학생과 모든 출석 기록을 삭제하시겠습니까?',
    clearAttendanceConfirm: '이 날의 출석 기록을 모두 초기화하시겠습니까?',
    noAttendance:           '출석 기록이 없습니다.',
    noStats:                '통계 데이터가 충분하지 않습니다.',
    loginFailed:            '로그인에 실패했습니다: {{msg}}',
    logoutFailed:           '로그아웃 실패: {{msg}}',
    migrateFailed:          '마이그레이션 실패: {{msg}}',
    migrateSuccess:         '기기 데이터를 성공적으로 가져왔습니다.',
    migrateReadFailed:      '기존 데이터를 읽을 수 없습니다.',
    periodAlertError:       '기간제 기준은 1일 이상이어야 합니다.',
    countAlertError:        '횟수제 기준은 0 이상이어야 합니다.',
    alertSaved:             '알림 기준이 저장되었습니다.',
    pageLoadFailed:         '페이지를 불러올 수 없습니다',
    unknownError:           '알 수 없는 오류',
    pageNotFound:           '페이지를 찾을 수 없습니다',
    goHome:                 '홈으로 돌아가기',
  },
  placeholders: {
    groupName:   '예: 3학년 2반, 피아노 교실',
    groupDesc:   '그룹에 대한 간단한 설명 (선택)',
    studentName: '학생 이름',
    studentMemo: '메모 (선택)',
    search:      '이름 검색...',
  },
  modal: {
    confirm: '확인',
    cancel:  '취소',
  },
};
```

- [ ] **Step 2: `js/locales/en.js` 생성**

```js
/**
 * English locale strings
 */
export default {
  nav: {
    home:         'Home',
    groups:       'Groups',
    calendar:     'Calendar',
    stats:        'Stats',
    settings:     'Settings',
    dashboard:    'Dashboard',
    groupMgmt:    'Manage Groups',
    myGroups:     'My Groups',
    addGroup:     '+ Add Group',
    noGroupsShort: 'No groups',
    mainMenu:     'Main Menu',
    bottomNav:    'Bottom Navigation',
  },
  status: {
    present: 'Present',
    absent:  'Absent',
    late:    'Late',
    early:   'Early Leave',
    none:    'Not Set',
  },
  date: {
    today:     'Today',
    yesterday: 'Yesterday',
    daysShort: ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'],
    months:    ['January', 'February', 'March', 'April', 'May', 'June',
                'July', 'August', 'September', 'October', 'November', 'December'],
    full:      '{month} {day}, {year} ({dow})',
    short:     '{month}/{day}({dow})',
    monthDay:  '{month} {day} ({dow})',
    yearMonth: '{month} {year}',
  },
  common: {
    back:          'Back',
    save:          'Save',
    cancel:        'Cancel',
    confirm:       'OK',
    delete:        'Delete',
    edit:          'Edit',
    add:           'Add',
    close:         'Close',
    loading:       'Loading...',
    optional:      '(optional)',
    required:      '*',
    noData:        'No data',
    groupNotFound: 'Group not found',
  },
  dashboard: {
    title:            'Dashboard',
    totalGroups:      'Total Groups',
    totalStudents:    'Total Students',
    todayPresent:     "Today's Present",
    todayAbsent:      "Today's Absent",
    todayStatus:      "Today's Group Status",
    manageAll:        'Manage All →',
    contractExpiry:   '⚠ Expiring Contracts',
    criteriaSettings: 'Settings →',
    periodBadge:      'Period',
    countBadge:       'Session',
    endsToday:        'Ends Today',
    daysLeft:         '{{n}} days left',
    exhausted:        'Used Up',
    remaining:        '{{n}} left',
    manage:           'Manage →',
    attendCheck:      'Attendance',
    studentCount:     '{{n}}',
    present:          'Present',
    absent:           'Absent',
    notEntered:       'Not Set',
    welcome:          'Welcome!',
    welcomeDesc:      'Create a group to start tracking attendance.',
    firstGroup:       '+ Create First Group',
  },
  groups: {
    title:               'Groups',
    addGroup:            '+ New Group',
    noGroups:            'No groups',
    noGroupsDesc:        'Create a new group to manage your students.',
    addGroupBtn:         '+ Create New Group',
    addGroupCard:        'Add Group',
    newGroupTitle:       'New Group',
    editGroupTitle:      'Edit Group',
    groupName:           'Group Name',
    groupDesc:           'Description',
    color:               'Color',
    create:              'Create',
    studentCountLoading: 'Loading...',
    studentCount:        '{{n}} students',
  },
  groupDetail: {
    memberCount:    '{{n}} members',
    todayStatus:    '{{monthDay}} attendance',
    attendCheck:    "Today's Attendance",
    addMember:      '+ Add Member',
    memberList:     'Members',
    addShort:       '+ Add',
    noMembers:      'No members',
    noMembersDesc:  'Add members to start tracking attendance.',
    addMemberEmpty: '+ Add Member',
    searchNoResult: 'No results for "{{query}}".',
    backToGroups:   'Back to Groups',
    attendLink:     '📋 Attendance',
    calendarLink:   '📅 Calendar',
    statsLink:      '📊 Statistics',
    genderMale:     'M',
    genderFemale:   'F',
    genderOther:    'Other',
    colNumber:      '#',
    colName:        'Name',
    colAge:         'Age',
    colGender:      'Gender',
    colPhone:       'Phone',
    colDays:        'Days',
    colTime:        'Time',
    colReg:         'Registered',
    colContract:    'Contract',
    colToday:       'Today',
    colManage:      'Manage',
    addError:       'Error: {{msg}}',
  },
  attendance: {
    title:       'Attendance',
    prevDay:     'Previous Day',
    nextDay:     'Next Day',
    pickDate:    'Pick Date',
    allStudents: 'All:',
    markAll:     'All {{status}}',
    markSched:   'Sched. {{status}}',
    scheduled:   'Scheduled Today',
    unscheduled: 'Not Scheduled',
    count:       '{{n}}',
    clearAll:    'Reset',
    noStudents:  'No students',
    addStudents: 'Add students',
  },
  statistics: {
    title:        'Statistics',
    calculating:  'Calculating...',
    monthlyRate:  'Monthly Rate',
    studentCount: 'Students',
    classDays:    'Class Days',
    avgAbsent:    'Avg. Absent',
    dailyChart:   'Daily Attendance',
    studentTable: 'Per-Student Attendance',
    colNumber:    '#',
    colName:      'Name',
    colPresent:   'Present',
    colAbsent:    'Absent',
    colLate:      'Late',
    colEarly:     'Early',
    colRate:      'Rate',
    noData:       'No data',
  },
  calendar: {
    title:    'Calendar',
    allTitle: '📅 Calendar',
    loading:  'Loading calendar...',
  },
  export: {
    title:     'Export',
    dateRange: 'Date Range',
    startDate: 'Start Date',
    endDate:   'End Date',
    csvBtn:    '📄 Export CSV',
    pdfBtn:    '📑 Export PDF',
  },
  settings: {
    title:             'Settings',
    account:           'Account',
    language:          'Language',
    systemLanguage:    'Use System Language',
    localMode:         'Local Mode',
    localModeDesc:     'Data is stored on this device only',
    loginGoogleDesc:   'Sign in with Google to back up your data to the cloud and sync across all devices.',
    loginGoogle:       'Sign in with Google',
    logout:            'Sign Out',
    logoutConfirm:     'Are you sure you want to sign out?',
    noName:            'No Name',
    migrationTitle:    'Import Legacy Data',
    migrationDesc:     'Data from an older version was detected. Tap the button below to import it to the cloud.',
    migrateBtn:        'Import Device Data',
    migratingBtn:      'Importing...',
    migrateModalTitle: 'Import Device Data',
    migrateModalMsg:   'This will import your locally stored data to the cloud.\nAll current cloud data will be replaced.\nContinue?',
    migrateModalBtn:   'Import',
    contractAlert:     'Contract Expiry Alert Thresholds',
    periodType:        'Period-based',
    periodTypeDesc:    'Show students whose end date is within N days',
    daysUnit:          'days or less',
    countType:         'Session-based',
    countTypeDesc:     'Show students with N or fewer sessions remaining',
    countUnit:         'sessions or less',
    saveBtn:           'Save',
    appInfo:           'App Info',
    version:           'Version',
    developer:         'Developer',
  },
  login: {
    subtitle:     'Manage student attendance quickly and easily',
    googleSignIn: 'Sign in with Google',
    signingIn:    'Signing in...',
    or:           'or',
    guestMode:    'Continue without signing in',
    termsText:    'By continuing, you agree to our {{terms}} and {{privacy}}.',
    terms:        'Terms of Service',
    privacy:      'Privacy Policy',
  },
  premium: {
    title:        'Premium',
    upgradeTitle: 'Upgrade to Premium',
    alreadyTitle: "You're already a Premium user!",
    alreadyDesc:  'You have access to all features without restrictions.',
    goHome:       'Go Home',
    heroTitle:    'Upgrade to Premium',
    heroDesc:     'Unlock all features without limits',
    lifetime:     'Lifetime Access',
    f1Icon:  '👥', f1Title: 'Unlimited Groups & Students', f1Desc: 'No limits on groups or student count',
    f2Icon:  '📊', f2Title: 'Advanced Statistics',         f2Desc: 'Daily and monthly attendance charts',
    f3Icon:  '📄', f3Title: 'CSV/PDF Export',              f3Desc: 'Export attendance data to Excel or PDF',
    f4Icon:  '🚫', f4Title: 'No Ads',                      f4Desc: 'Enjoy an ad-free experience',
  },
  messages: {
    groupCreated:           'Group created.',
    groupUpdated:           'Group updated.',
    groupDeleted:           'Group deleted.',
    studentCreated:         'Student added.',
    studentUpdated:         'Student updated.',
    studentDeleted:         'Student deleted.',
    attendanceSaved:        'Attendance saved.',
    exportDone:             'Export complete',
    backupSaved:            'Backup file saved.',
    backupLoaded:           'Backup loaded.',
    premiumActivated:       'Premium activated! 🎉',
    loadFailed:             'Failed to load data.',
    saveFailed:             'Failed to save.',
    nameRequired:           'Please enter a name.',
    invalidFile:            'Invalid file format.',
    freeGroupLimit:         'Free plan allows 1 group.\nUpgrade to Premium for unlimited groups.',
    freeStudentLimit:       'Free plan allows 20 students per group.\nUpgrade to Premium for unlimited students.',
    premiumRequired:        'This feature is available on the Premium plan.',
    deleteGroupConfirm:     'Deleting "{{name}}" will also delete all students and attendance records. Continue?',
    deleteStudentConfirm:   'Delete "{{name}}" and all their attendance records?',
    clearAttendanceConfirm: 'Reset all attendance records for this day?',
    noAttendance:           'No attendance records.',
    noStats:                'Not enough data for statistics.',
    loginFailed:            'Sign in failed: {{msg}}',
    logoutFailed:           'Sign out failed: {{msg}}',
    migrateFailed:          'Migration failed: {{msg}}',
    migrateSuccess:         'Device data imported successfully.',
    migrateReadFailed:      'Could not read legacy data.',
    periodAlertError:       'Period threshold must be at least 1 day.',
    countAlertError:        'Session threshold must be 0 or more.',
    alertSaved:             'Alert settings saved.',
    pageLoadFailed:         'Could not load page',
    unknownError:           'Unknown error',
    pageNotFound:           'Page not found',
    goHome:                 'Back to Home',
  },
  placeholders: {
    groupName:   'e.g. Grade 3 Class 2, Piano Room',
    groupDesc:   'Brief description (optional)',
    studentName: 'Student name',
    studentMemo: 'Notes (optional)',
    search:      'Search by name...',
  },
  modal: {
    confirm: 'OK',
    cancel:  'Cancel',
  },
};
```

- [ ] **Step 3: 커밋**

```bash
cd C:/test/attendance-app
git add js/locales/ko.js js/locales/en.js
git commit -m "feat: add ko/en locale files"
```

---

## Task 2: `js/utils/i18n.js` 재작성

**Files:**
- Modify: `js/utils/i18n.js` (complete rewrite)

- [ ] **Step 1: i18n.js 완전 재작성**

```js
/**
 * i18n utility
 *
 * API:
 *   await initLocale()           — app.js 시작 시 한 번 호출
 *   t('nav.home')                — 현재 언어 문자열 반환
 *   t('messages.daysLeft', {n}) — {{n}} 변수 치환
 *   getLocale()                  — 현재 언어 코드 ('ko' | 'en')
 *   getMessages()                — 현재 로케일 전체 객체 (배열 접근 시)
 *   await setLocale('en')        — 언어 변경 + IndexedDB 저장 + localechange 이벤트
 */

import koMessages from '../locales/ko.js';
import enMessages from '../locales/en.js';
import { getByKey, put } from '../db/database.js';

const LOCALES = { ko: koMessages, en: enMessages };
export const SUPPORTED_LOCALES = ['ko', 'en'];

let _locale   = 'ko';
let _messages = koMessages;

/** 앱 시작 시 한 번 호출 — IndexedDB 또는 navigator.language 로 로케일 결정 */
export async function initLocale() {
  try {
    const saved = await getByKey('settings', 'locale');
    const savedValue = saved?.value;
    if (savedValue && savedValue !== 'system' && SUPPORTED_LOCALES.includes(savedValue)) {
      _locale = savedValue;
    } else {
      _locale = _detectFromNavigator();
    }
  } catch {
    _locale = _detectFromNavigator();
  }
  _messages = LOCALES[_locale];
}

function _detectFromNavigator() {
  const lang = (navigator.language || 'ko').split('-')[0].toLowerCase();
  return SUPPORTED_LOCALES.includes(lang) ? lang : 'ko';
}

/** 현재 언어 코드 반환 */
export function getLocale() {
  return _locale;
}

/** 현재 로케일 전체 객체 반환 (배열 등 직접 접근 시) */
export function getMessages() {
  return _messages;
}

/** 언어 변경 + IndexedDB 저장 + localechange 이벤트 발송 */
export async function setLocale(code) {
  const value = code; // 'ko' | 'en' | 'system'
  await put('settings', { key: 'locale', value });
  if (code === 'system') {
    _locale = _detectFromNavigator();
  } else if (SUPPORTED_LOCALES.includes(code)) {
    _locale = code;
  } else {
    _locale = 'ko';
  }
  _messages = LOCALES[_locale];
  window.dispatchEvent(new CustomEvent('localechange', { detail: { locale: _locale } }));
}

/**
 * 번역 함수
 * @param {string} key  — 점 표기 키 e.g. 'nav.home', 'messages.daysLeft'
 * @param {object} vars — 치환 변수 e.g. { n: 3 }  → {{n}} 치환
 * @returns {string}    — 번역된 문자열, 없으면 key 그대로 반환
 */
export function t(key, vars) {
  const parts = key.split('.');
  let val = _messages;
  for (const p of parts) {
    if (val == null || typeof val !== 'object') { val = undefined; break; }
    val = val[p];
  }
  if (val == null || typeof val !== 'string') return key;
  if (!vars) return val;
  return val.replace(/\{\{(\w+)\}\}/g, (_, k) => (vars[k] != null ? vars[k] : ''));
}

// ─── 하위 호환 내보내기 (기존 코드가 import하는 것들) ──────────────────────────

/** @deprecated t('status.*') 사용 권장 */
export const STATUS_LABELS = new Proxy({}, {
  get(_, key) { return t('status.' + key); },
});

export const STATUS_LIST   = ['present', 'absent', 'late', 'early'];

export const GROUP_COLORS = [
  '#4F46E5', '#10B981', '#F59E0B', '#EF4444',
  '#8B5CF6', '#06B6D4', '#EC4899', '#84CC16',
  '#F97316', '#14B8A6',
];

export const FREE_LIMITS = { groups: 1, students: 20 };

export const PREMIUM_PRICE = '₩9,900';

/** @deprecated t('messages.*') 사용 권장 */
export const MESSAGES = new Proxy({}, {
  get(_, key) {
    const raw = _messages?.messages?.[key];
    if (typeof raw === 'string') {
      // 함수 형태로 접근하는 기존 코드 호환 (e.g. MESSAGES.deleteGroupConfirm(name))
      return (param) => {
        if (param !== undefined) return t('messages.' + key, { name: param });
        return t('messages.' + key);
      };
    }
    return () => t('messages.' + key);
  },
});

/** @deprecated t('placeholders.*') 사용 권장 */
export const PLACEHOLDERS = new Proxy({}, {
  get(_, key) { return t('placeholders.' + key); },
});

/** @deprecated t('nav.*') 사용 권장 */
export const NAV_LABELS = new Proxy({}, {
  get(_, key) { return t('nav.' + key); },
});
```

> **주의:** `MESSAGES` Proxy의 함수 호환은 기존 코드(`MESSAGES.deleteGroupConfirm(group.name)`)가 계속 동작하게 하기 위함입니다. 모든 페이지 파일이 `t()` 직접 호출로 마이그레이션 완료된 후 Proxy 하위 호환 코드는 제거해도 됩니다.

- [ ] **Step 2: 커밋**

```bash
cd C:/test/attendance-app
git add js/utils/i18n.js
git commit -m "feat: rewrite i18n.js with t(), initLocale(), setLocale()"
```

---

## Task 3: `js/utils/date.js` 업데이트

**Files:**
- Modify: `js/utils/date.js`

`formatDateKo`, `formatYearMonthKo`, `relativeDateLabel` 함수를 locale-aware 버전으로 교체. 기존 함수 이름은 alias로 유지.

- [ ] **Step 1: date.js 수정 — 해당 함수들 교체**

파일 맨 위에 import 추가:
```js
import { getLocale, getMessages } from './i18n.js';
```

`formatDateKo` 함수 교체 (기존 함수를 아래로 대체):
```js
/**
 * Formats a YYYY-MM-DD string using the current locale.
 * options.short     → short format (e.g. "3/15(금)" or "3/15(Fri)")
 * options.monthDay  → month+day format
 */
export function formatDate(str, options = {}) {
  const date = strToDate(str);
  const msgs = getMessages();
  const locale = getLocale();
  const dow = msgs.date.daysShort[date.getDay()];
  const y = date.getFullYear();
  const mo = date.getMonth();
  const d = date.getDate();
  // For Korean: numeric month; for English: month name
  const monthDisplay = locale === 'ko' ? (mo + 1) : msgs.date.months[mo];

  let template;
  if (options.short)    template = msgs.date.short;
  else if (options.monthDay) template = msgs.date.monthDay;
  else                  template = msgs.date.full;

  return template
    .replace('{year}',  y)
    .replace('{month}', monthDisplay)
    .replace('{day}',   d)
    .replace('{dow}',   dow);
}

/** Alias for backward compatibility */
export function formatDateKo(str, options = {}) {
  return formatDate(str, options);
}
```

`formatYearMonthKo` 함수 교체:
```js
/**
 * Formats YYYY-MM string using current locale.
 * e.g. "2024-03" → "2024년 3월" (ko) or "March 2024" (en)
 */
export function formatYearMonth(yearMonthStr) {
  const msgs = getMessages();
  const locale = getLocale();
  const [y, m] = yearMonthStr.split('-').map(Number);
  const monthDisplay = locale === 'ko' ? m : msgs.date.months[m - 1];
  return msgs.date.yearMonth
    .replace('{year}',  y)
    .replace('{month}', monthDisplay);
}

/** Alias for backward compatibility */
export function formatYearMonthKo(yearMonthStr) {
  return formatYearMonth(yearMonthStr);
}
```

`relativeDateLabel` 함수 교체:
```js
/**
 * Returns relative label: "오늘"/"Today", "어제"/"Yesterday", or short formatted date
 */
export function relativeDateLabel(dateStr) {
  const msgs = getMessages();
  const today = todayStr();
  const yesterday = shiftDate(today, -1);
  if (dateStr === today)     return msgs.date.today;
  if (dateStr === yesterday) return msgs.date.yesterday;
  return formatDate(dateStr, { short: true });
}
```

- [ ] **Step 2: 커밋**

```bash
cd C:/test/attendance-app
git add js/utils/date.js
git commit -m "feat: locale-aware date formatting in date.js"
```

---

## Task 4: `js/app.js` + `js/router.js` 업데이트

**Files:**
- Modify: `js/app.js`
- Modify: `js/router.js`

### app.js 변경사항

- [ ] **Step 1: app.js에 import 추가 및 initLocale 호출**

파일 상단 import 섹션에 추가:
```js
import { initLocale } from './utils/i18n.js';
```

`_initApp()` 함수 맨 첫 줄에 추가 (try 블록 안, `PremiumService.load()` 전):
```js
// 0. Init locale (must be first — all UI strings depend on this)
await initLocale();
```

`_initApp()` 함수 내 `_router.start()` 호출 바로 뒤에 추가:
```js
// locale change → re-render current page + nav components
window.addEventListener('localechange', () => {
  Sidebar.render();
  BottomNav.render();
  _router.refresh();
});
```

### router.js 변경사항

- [ ] **Step 2: router.js에 t() import + 에러 메시지 교체**

파일 상단에 추가:
```js
import { t } from './utils/i18n.js';
```

`_resolve()` 내 에러 catch 블록 교체:
```js
} catch (e) {
  console.error('[Router] page error:', e);
  if (this._container) {
    this._container.innerHTML = `
      <div class="empty-state">
        <div class="empty-state-icon">⚠</div>
        <div class="empty-state-title">${t('messages.pageLoadFailed')}</div>
        <div class="empty-state-desc">${e.message || t('messages.unknownError')}</div>
      </div>`;
  }
}
```

404 블록 교체:
```js
if (this._container) {
  this._container.innerHTML = `
    <div class="empty-state">
      <div class="empty-state-icon">🔍</div>
      <div class="empty-state-title">${t('messages.pageNotFound')}</div>
      <div class="empty-state-desc"><a href="#/">${t('messages.goHome')}</a></div>
    </div>`;
}
```

- [ ] **Step 3: 커밋**

```bash
cd C:/test/attendance-app
git add js/app.js js/router.js
git commit -m "feat: call initLocale() at startup, localechange re-render"
```

---

## Task 5: 컴포넌트 업데이트 (modal, bottom-nav, sidebar)

**Files:**
- Modify: `js/components/modal.js`
- Modify: `js/components/bottom-nav.js`
- Modify: `js/components/sidebar.js`

### modal.js

- [ ] **Step 1: modal.js import 추가 + 기본 버튼 텍스트 교체**

파일 상단 import에 추가:
```js
import { t } from '../utils/i18n.js';
```

`open()` 함수 시그니처의 기본값 교체:
```js
open({ title, body, onConfirm, confirmText, cancelText, danger = false, hideCancel = false, hideConfirm = false }) {
  // 기본값은 open() 안에서 t()로 설정 (import 시점에 로케일 미초기화 방지)
  confirmText = confirmText ?? t('modal.confirm');
  cancelText  = cancelText  ?? t('modal.cancel');
```

### bottom-nav.js

- [ ] **Step 2: bottom-nav.js 전체 교체**

```js
/**
 * Bottom navigation bar (mobile only)
 */

import { escapeHtml } from '../utils/dom.js';
import { t } from '../utils/i18n.js';

const NAV_ITEMS = [
  { icon: '⊞', labelKey: 'nav.home',     hash: '#/',         key: 'home' },
  { icon: '👥', labelKey: 'nav.groups',   hash: '#/groups',   key: 'groups' },
  { icon: '📅', labelKey: 'nav.calendar', hash: '#/calendar', key: 'calendar' },
  { icon: '📊', labelKey: 'nav.stats',    hash: '#/stats',    key: 'stats' },
  { icon: '⚙',  labelKey: 'nav.settings', hash: '#/settings', key: 'settings' },
];

export const BottomNav = {
  _el: null,

  init() {
    this._el = document.querySelector('.bottom-nav');
    if (!this._el) return;
    this.render();
    window.addEventListener('hashchange', () => this._updateActive());
  },

  render() {
    if (!this._el) return;
    this._el.innerHTML = `
      <nav class="bottom-nav-inner" aria-label="${escapeHtml(t('nav.bottomNav'))}">
        ${NAV_ITEMS.map(item => {
          const label = t(item.labelKey);
          return `
          <a href="${escapeHtml(item.hash)}"
             class="bottom-nav-item${this._isActive(item.hash) ? ' active' : ''}"
             data-key="${item.key}"
             aria-label="${escapeHtml(label)}"
             aria-current="${this._isActive(item.hash) ? 'page' : 'false'}">
            <span class="bottom-nav-icon" aria-hidden="true">${item.icon}</span>
            <span class="bottom-nav-label">${escapeHtml(label)}</span>
          </a>`;
        }).join('')}
      </nav>
    `;
  },

  _updateActive() {
    if (!this._el) return;
    this._el.querySelectorAll('.bottom-nav-item').forEach(item => {
      const hash = item.getAttribute('href');
      const active = this._isActive(hash);
      item.classList.toggle('active', active);
      item.setAttribute('aria-current', active ? 'page' : 'false');
    });
  },

  _isActive(hash) {
    const current = window.location.hash || '#/';
    if (hash === '#/') return current === '#/' || current === '#';
    return current.startsWith(hash);
  },
};

export default BottomNav;
```

### sidebar.js

- [ ] **Step 3: sidebar.js NAV_ITEMS + 하드코딩 문자열 교체**

파일 상단 import에 추가:
```js
import { t } from '../utils/i18n.js';
```

`NAV_ITEMS` 배열 교체:
```js
const NAV_ITEMS = [
  { icon: '⊞', labelKey: 'nav.dashboard', hash: '#/' },
  { icon: '👥', labelKey: 'nav.groupMgmt', hash: '#/groups' },
  { icon: '📅', labelKey: 'nav.calendar',  hash: '#/calendar' },
  { icon: '📊', labelKey: 'nav.stats',     hash: '#/stats' },
  { icon: '⚙',  labelKey: 'nav.settings',  hash: '#/settings' },
];
```

`render()` 메서드 내 HTML 템플릿 교체:
```js
this._el.innerHTML = `
  <nav class="sidebar-nav" aria-label="${escapeHtml(t('nav.mainMenu'))}">
    ${NAV_ITEMS.map(item => `
      <a href="${item.hash}"
         class="sidebar-nav-item${this._isActive(item.hash, current) ? ' active' : ''}"
         aria-current="${this._isActive(item.hash, current) ? 'page' : 'false'}">
        <span class="nav-icon" aria-hidden="true">${item.icon}</span>
        ${escapeHtml(t(item.labelKey))}
      </a>
    `).join('')}

    <div class="sidebar-section-label">${escapeHtml(t('nav.myGroups'))}</div>
    <div class="sidebar-group-list">
      ${groups.map(g => `
        <a href="#/groups/${g.id}"
           class="sidebar-group-item${current.startsWith('#/groups/' + g.id) ? ' active' : ''}"
           data-group-id="${g.id}">
          <span class="sidebar-group-dot" style="background:${escapeHtml(g.color)}"></span>
          <span class="sidebar-group-name">${escapeHtml(g.name)}</span>
        </a>
      `).join('')}
      ${groups.length === 0 ? `<div style="padding: 8px 12px; font-size: 13px; color: var(--color-text-muted);">${escapeHtml(t('nav.noGroupsShort'))}</div>` : ''}
      <a href="#/groups" class="sidebar-nav-item" style="margin-top:4px; font-size:12px; color: var(--color-primary);">
        ${escapeHtml(t('nav.addGroup'))}
      </a>
    </div>
  </nav>
`;
```

- [ ] **Step 4: 커밋**

```bash
cd C:/test/attendance-app
git add js/components/modal.js js/components/bottom-nav.js js/components/sidebar.js
git commit -m "feat: i18n in modal, bottom-nav, sidebar components"
```

---

## Task 6: `js/pages/login.js` 업데이트

**Files:**
- Modify: `js/pages/login.js`

- [ ] **Step 1: import 교체 + render() 내 한국어 → t()**

파일 상단에 추가:
```js
import { t } from '../utils/i18n.js';
```

`render()` 반환 HTML 교체:
```js
render() {
  return `
    <div class="login-page">
      <div class="login-bg" aria-hidden="true">
        <div class="login-bg-blob login-bg-blob--top"></div>
        <div class="login-bg-blob login-bg-blob--bottom"></div>
      </div>
      <div class="login-container">
        <div class="login-brand">
          <div class="login-logo">출</div>
          <h1 class="login-title">출석부</h1>
          <p class="login-subtitle">${t('login.subtitle')}</p>
        </div>
        <div class="login-card">
          <button id="google-signin-btn" class="login-btn-google">
            ${GOOGLE_SVG}
            <span>${t('login.googleSignIn')}</span>
          </button>
          <div class="login-divider">
            <span class="login-divider-line"></span>
            <span class="login-divider-text">${t('login.or')}</span>
            <span class="login-divider-line"></span>
          </div>
          <button id="guest-mode-btn" class="login-btn-guest">
            ${t('login.guestMode')}
          </button>
          <div class="login-legal">
            <p>${t('login.termsText', {
              terms:   `<a href="#" class="login-legal-link" id="terms-link">${t('login.terms')}</a>`,
              privacy: `<a href="#" class="login-legal-link" id="privacy-link">${t('login.privacy')}</a>`,
            })}</p>
          </div>
        </div>
      </div>
    </div>
  `;
}
```

로그인 버튼 로딩 상태 (mount() 내) 교체:
```js
googleBtn.innerHTML = `<span style="display:inline-block;width:18px;height:18px;border:2px solid #f5d5c3;border-top-color:#D97757;border-radius:50%;animation:spin .7s linear infinite;"></span><span>${t('login.signingIn')}</span>`;
```

로그인 실패 Toast 교체:
```js
if (!silent) {
  Toast.error(t('messages.loginFailed', { msg: e.message || e.code || '' }));
}
```

버튼 복구:
```js
googleBtn.innerHTML = `${GOOGLE_SVG}<span>${t('login.googleSignIn')}</span>`;
```

- [ ] **Step 2: 커밋**

```bash
cd C:/test/attendance-app
git add js/pages/login.js
git commit -m "feat: i18n in login page"
```

---

## Task 7: `js/pages/dashboard.js` 업데이트

**Files:**
- Modify: `js/pages/dashboard.js`

- [ ] **Step 1: import 교체**

기존:
```js
import { STATUS_LABELS } from '../utils/i18n.js';
```
교체:
```js
import { t } from '../utils/i18n.js';
```

`formatDateKo` import는 그대로 유지 (date.js의 alias가 locale-aware하게 업데이트됨).

- [ ] **Step 2: render() 내 하드코딩 교체**

`render()` 반환 HTML 중 한국어 문자열 교체:
```js
// 페이지 헤더
<h1 class="page-title">${t('dashboard.title')}</h1>

// 통계 카드 라벨
<div class="stat-card-label">${t('dashboard.totalGroups')}</div>
<div class="stat-card-label">${t('dashboard.totalStudents')}</div>
<div class="stat-card-label">${t('dashboard.todayPresent')}</div>
<div class="stat-card-label">${t('dashboard.todayAbsent')}</div>

// 그룹 현황 카드 헤더
<div class="card-title">${t('dashboard.todayStatus')}</div>
<a href="#/groups" class="btn btn-ghost btn-sm" style="font-size:12px;">${t('dashboard.manageAll')}</a>
```

`_expiringCard()` 내:
```js
// 타입 뱃지
const typeBadge = type === 'period'
  ? `<span ...>${t('dashboard.periodBadge')}</span>`
  : `<span ...>${t('dashboard.countBadge')}</span>`;

// 상태 텍스트
const statusText = type === 'period'
  ? (daysLeft === 0 ? t('dashboard.endsToday') : t('dashboard.daysLeft', { n: daysLeft }))
  : (remaining <= 0 ? t('dashboard.exhausted') : t('dashboard.remaining', { n: remaining }));

// 카드 제목
<div class="card-title" style="color:var(--color-late);">${t('dashboard.contractExpiry')}
<a href="#/settings" ...>${t('dashboard.criteriaSettings')}</a>

// 관리 버튼
<a href="#/groups/${group.id}" class="btn btn-ghost btn-sm" style="flex-shrink:0; font-size:12px;">${t('dashboard.manage')}</a>
```

`_emptyGroups()` 내:
```js
<div class="empty-state-title">${t('dashboard.welcome')}</div>
<div class="empty-state-desc">${t('dashboard.welcomeDesc')}</div>
<a href="#/groups" class="btn btn-primary" style="margin-top: 12px;">${t('dashboard.firstGroup')}</a>
```

`_groupRow()` 내:
```js
// 출석 요약
<span class="att-summary-item">
  <span class="att-summary-dot" style="background:var(--color-present)"></span>
  ${t('dashboard.present')} ${present}
</span>
<span class="att-summary-item">
  <span class="att-summary-dot" style="background:var(--color-absent)"></span>
  ${t('dashboard.absent')} ${absent}
</span>
${none > 0 ? `<span class="att-summary-item">
  <span class="att-summary-dot" style="background:var(--color-text-muted)"></span>
  ${t('dashboard.notEntered')} ${none}
</span>` : ''}

// 학생수 / 출석 체크 버튼
<div style="font-size: 12px; color: var(--color-text-muted);">${t('dashboard.studentCount', { n: students })}</div>
<a href="#/groups/${group.id}/attend" class="btn btn-primary btn-sm" style="flex-shrink:0;">${t('dashboard.attendCheck')}</a>
```

- [ ] **Step 3: 커밋**

```bash
cd C:/test/attendance-app
git add js/pages/dashboard.js
git commit -m "feat: i18n in dashboard page"
```

---

## Task 8: `js/pages/groups.js` 업데이트

**Files:**
- Modify: `js/pages/groups.js`

- [ ] **Step 1: import 교체**

기존:
```js
import { GROUP_COLORS, MESSAGES, PLACEHOLDERS } from '../utils/i18n.js';
```
교체:
```js
import { GROUP_COLORS, t } from '../utils/i18n.js';
```

- [ ] **Step 2: render() + 모든 메서드 내 한국어 교체**

`render()`:
```js
<h1 class="page-title">${t('groups.title')}</h1>
<button class="btn btn-primary" id="add-group-btn">${t('groups.addGroup')}</button>
```

`_emptyState()`:
```js
<div class="empty-state-title">${t('groups.noGroups')}</div>
<div class="empty-state-desc">${t('groups.noGroupsDesc')}</div>
<button class="btn btn-primary" id="add-group-empty-btn" style="margin-top: 8px;">${t('groups.addGroupBtn')}</button>
```

`_groupGrid()` 내 추가 카드:
```js
<div style="font-size: 14px; font-weight: 600;">${t('groups.addGroupCard')}</div>
```

`_groupCard()` 내:
```js
<button ... title="${t('common.edit')}" aria-label="${t('common.edit')}">✎</button>
<button ... title="${t('common.delete')}" aria-label="${t('common.delete')}" ...>✕</button>
<span>${t('groups.studentCountLoading')}</span>  // 초기 로딩 텍스트
```

`_loadStudentCounts()` 내:
```js
if (meta) meta.innerHTML = `<span>${t('groups.studentCount', { n: cnt })}</span>`;
```

`_openAddModal()`:
```js
Modal.open({
  title: t('groups.newGroupTitle'),
  confirmText: t('groups.create'),
  ...
});
```

`_openEditModal()`:
```js
Modal.open({
  title: t('groups.editGroupTitle'),
  confirmText: t('common.save'),
  ...
});
```

`_groupForm()`:
```js
<label class="form-label" for="group-name">${t('groups.groupName')} <span style="color:var(--color-absent)">${t('common.required')}</span></label>
<input ... placeholder="${t('placeholders.groupName')}" ...>

<label class="form-label" for="group-desc">${t('groups.groupDesc')} <span class="form-label-optional">${t('common.optional')}</span></label>
<input ... placeholder="${t('placeholders.groupDesc')}" ...>

<label class="form-label">${t('groups.color')}</label>
```

`_submitAdd()`:
```js
if (!name) { Toast.error(t('messages.nameRequired')); return; }
Toast.success(t('messages.groupCreated'));
// catch:
Toast.error(t('messages.saveFailed'));
```

`_submitEdit()`:
```js
if (!name) { Toast.error(t('messages.nameRequired')); return; }
Toast.success(t('messages.groupUpdated'));
Toast.error(t('messages.saveFailed'));
```

`_deleteGroup()`:
```js
Modal.confirm({
  title: t('groups.deleteGroup') || t('common.delete'),
  message: t('messages.deleteGroupConfirm', { name: group.name }),
  danger: true,
  confirmText: t('common.delete'),
});
Toast.success(t('messages.groupDeleted'));
Toast.error(t('messages.saveFailed'));
```

- [ ] **Step 3: 커밋**

```bash
cd C:/test/attendance-app
git add js/pages/groups.js
git commit -m "feat: i18n in groups page"
```

---

## Task 9: `js/pages/group-detail.js` 업데이트

**Files:**
- Modify: `js/pages/group-detail.js`

- [ ] **Step 1: import 교체 + 상수 교체**

기존:
```js
import { STATUS_LABELS, MESSAGES } from '../utils/i18n.js';
const DAYS = ['월', '화', '수', '목', '금', '토', '일'];
const GENDER_LABELS = { male: '남', female: '여', other: '기타' };
```
교체:
```js
import { t } from '../utils/i18n.js';
// DAYS는 DB 저장값 매칭용 → 변경 없음 (locale 무관)
const DAYS = ['월', '화', '수', '목', '금', '토', '일'];
// GENDER_LABELS: 이제 t()로 접근
const genderLabel = (g) => ({
  male: t('groupDetail.genderMale'),
  female: t('groupDetail.genderFemale'),
  other: t('groupDetail.genderOther'),
}[g] || '—');
```

- [ ] **Step 2: render() 내 한국어 교체**

헤더:
```js
<div class="page-subtitle">${t('groupDetail.memberCount', { n: this.students.length })} · ${formatDate(this.today, { monthDay: true })} ${t('groupDetail.todayStatus').replace('{{monthDay}}', '')}</div>
// 더 깔끔하게:
<div class="page-subtitle">${t('groupDetail.todayStatus', { monthDay: formatDate(this.today, { monthDay: true }) })}</div>
```

버튼:
```js
<a href="#/groups/${this.groupId}/attend" class="btn btn-primary">${t('groupDetail.attendCheck')}</a>
<button class="btn btn-secondary" id="add-student-btn">${t('groupDetail.addMember')}</button>
```

요약 카드 (summaryCard 호출부):
```js
${this._summaryCard(t('status.present'), summary.present, 'var(--color-present)')}
${this._summaryCard(t('status.absent'),  summary.absent,  'var(--color-absent)')}
${this._summaryCard(t('status.late'),    summary.late,    'var(--color-late)')}
${this._summaryCard(t('status.none'),    summary.none,    'var(--color-text-muted)')}
```

빠른 이동 버튼:
```js
<a href="#/groups/${this.groupId}/attend" class="btn btn-outline btn-sm">${t('groupDetail.attendLink')}</a>
<a href="#/groups/${this.groupId}/calendar" class="btn btn-outline btn-sm">${t('groupDetail.calendarLink')}</a>
<a href="#/groups/${this.groupId}/stats" class="btn btn-outline btn-sm">${t('groupDetail.statsLink')}</a>
```

구성원 카드 헤더:
```js
<div class="card-title">${t('groupDetail.memberList')}</div>
<input ... placeholder="${t('placeholders.search')}" ...>
<button class="btn btn-primary btn-sm" id="add-student-card-btn">${t('groupDetail.addShort')}</button>
```

그룹 없음:
```js
<div class="empty-state-title">${t('common.groupNotFound')}</div>
<div class="empty-state-desc"><a href="#/groups">${t('groupDetail.backToGroups')}</a></div>
```

`_emptyStudents()`:
```js
<div class="empty-state-title">${t('groupDetail.noMembers')}</div>
<div class="empty-state-desc">${t('groupDetail.noMembersDesc')}</div>
<button class="btn btn-primary" id="add-student-empty-btn" style="margin-top: 8px;">${t('groupDetail.addMemberEmpty')}</button>
```

`_studentTable()` 헤더 행:
```js
<th ...>${t('groupDetail.colNumber')}</th>
<th ...>${t('groupDetail.colName')}</th>
<th ... class="col-age">${t('groupDetail.colAge')}</th>
<th ... class="col-gender">${t('groupDetail.colGender')}</th>
<th ... class="col-phone">${t('groupDetail.colPhone')}</th>
<th ... class="col-days">${t('groupDetail.colDays')}</th>
<th ... class="col-time">${t('groupDetail.colTime')}</th>
<th ... class="col-reg">${t('groupDetail.colReg')}</th>
<th ... class="col-contract">${t('groupDetail.colContract')}</th>
<th ...>${t('groupDetail.colToday')}</th>
<th ...>${t('groupDetail.colManage')}</th>
```

검색 결과 없음:
```js
<div class="empty-state-desc">${t('groupDetail.searchNoResult', { query: escapeHtml(this._searchQuery) })}</div>
```

`_studentRow()`:
```js
// 성별
<td ...>${genderLabel(s.gender)}</td>

// 오늘 출석 상태
${att
  ? `<span class="badge badge-${att.status}">${t('status.' + att.status)}</span>`
  : `<span class="badge badge-none">${t('status.none')}</span>`}

// 수정/삭제 버튼
<button ... title="${t('common.edit')}">✎</button>
<button ... title="${t('common.delete')}" ...>✕</button>
```

`mount()` 내 오류 Toast:
```js
Toast.error(t('groupDetail.addError', { msg: e.message || '구성원 추가에 실패했습니다.' }));
// 또는:
Toast.error(t('messages.saveFailed'));
```

학생 추가/수정/삭제 Toast + confirm 메시지도 모두 `t('messages.*')` 로 교체.

- [ ] **Step 3: 커밋**

```bash
cd C:/test/attendance-app
git add js/pages/group-detail.js
git commit -m "feat: i18n in group-detail page"
```

---

## Task 10: `js/pages/attendance.js` 업데이트

**Files:**
- Modify: `js/pages/attendance.js`

- [ ] **Step 1: import 교체**

기존:
```js
import { STATUS_LABELS, STATUS_LIST, MESSAGES } from '../utils/i18n.js';
import { ..., formatDateKo, ... } from '../utils/date.js';
```
교체:
```js
import { STATUS_LIST, t } from '../utils/i18n.js';
import { ..., formatDate, ... } from '../utils/date.js';
```

> `formatDateKo`는 alias이므로 그대로 써도 되나, 일관성을 위해 `formatDate`로 교체 권장.

- [ ] **Step 2: render() 내 한국어 교체**

헤더:
```js
<a ... aria-label="${t('common.back')}" ...>←</a>
<h1 class="page-title">${escapeHtml(this.group.name)} — ${t('attendance.title')}</h1>
<div class="page-subtitle">${formatDate(this.date)}</div>
<button ... id="prev-date" aria-label="${t('attendance.prevDay')}">←</button>
<button ... id="date-picker-trigger" title="${t('attendance.pickDate')}">${formatDate(this.date, { short: true })}</button>
<button ... id="next-date" aria-label="${t('attendance.nextDay')}">→</button>
```

요약 카드:
```js
${this._summaryCard(t('status.present'), summary.present, 'var(--color-present)')}
${this._summaryCard(t('status.absent'),  summary.absent,  'var(--color-absent)')}
${this._summaryCard(t('status.late'),    summary.late,    'var(--color-late)')}
${this._summaryCard(t('status.early'),   summary.early,   'var(--color-early)')}
```

퀵 마크 바:
```js
<span class="quick-mark-label">${t('attendance.allStudents')}</span>
${STATUS_LIST.map(s => `
  <button class="btn btn-sm btn-secondary quick-all-btn" data-status="${s}">
    ${t('attendance.markAll', { status: t('status.' + s) })}
  </button>
`).join('')}
<button class="btn btn-sm btn-ghost" id="clear-all-btn" ...>${t('attendance.clearAll')}</button>

// 예정만 바:
<span class="quick-mark-label" style="color:var(--color-present);">${t('attendance.allStudents')}</span>
${STATUS_LIST.map(s => `
  <button class="btn btn-sm btn-secondary quick-scheduled-btn" data-status="${s}">
    ${t('attendance.markSched', { status: t('status.' + s) })}
  </button>
`).join('')}
```

학생 없음 empty state:
```js
<div class="empty-state-title">${t('attendance.noStudents')}</div>
<div class="empty-state-desc"><a href="#/groups/${this.groupId}">${t('attendance.addStudents')}</a></div>
```

그룹 없음:
```js
<div class="empty-state-title">${t('common.groupNotFound')}</div>
```

`_renderGroupedList()` 내 섹션 헤더:
```js
this._sectionHeader(t('attendance.scheduled'),   this._scheduled.length,   'var(--color-present)')
this._sectionHeader(t('attendance.unscheduled'), this._unscheduled.length, 'var(--color-text-muted)')
```

`_sectionHeader()` 내 인원수 표시:
```js
<span ...>${t('attendance.count', { n: count })}</span>
```

`_studentCard()` 내 버튼:
```js
${STATUS_LIST.map(s => `
  <button class="att-btn${activeStatus === s ? ' active' : ''}"
    data-status="${s}"
    data-student-id="${student.id}"
    aria-pressed="${activeStatus === s ? 'true' : 'false'}"
    aria-label="${t('status.' + s)}">
    ${t('status.' + s)}
  </button>
`).join('')}
```

`mount()` 내 출석 초기화 confirm:
```js
const ok = await Modal.confirm({
  title: t('attendance.clearAll'),
  message: t('messages.clearAttendanceConfirm'),
  danger: true,
  confirmText: t('attendance.clearAll'),
});
```

- [ ] **Step 3: 커밋**

```bash
cd C:/test/attendance-app
git add js/pages/attendance.js
git commit -m "feat: i18n in attendance page"
```

---

## Task 11: `js/pages/statistics.js` 업데이트

**Files:**
- Modify: `js/pages/statistics.js`

- [ ] **Step 1: import 교체**

기존:
```js
import { ..., formatYearMonthKo, ... } from '../utils/date.js';
```
교체 (alias이므로 그대로 동작하나 일관성 위해):
```js
import { ..., formatYearMonth, ... } from '../utils/date.js';
import { t } from '../utils/i18n.js';
```

- [ ] **Step 2: render() + _renderStats() 내 한국어 교체**

`render()`:
```js
<h1 class="page-title">${escapeHtml(this.group.name)} — ${t('statistics.title')}</h1>
<span class="date-nav-label" id="month-label">${formatYearMonth(this.yearMonth)}</span>
<div class="empty-state-title">${t('common.groupNotFound')}</div>
```

`_changeMonth()`:
```js
if (labelEl) labelEl.textContent = formatYearMonth(this.yearMonth);
```

`_renderStats()` 내:
```js
// 요약 카드
<div class="stat-card-label">${t('statistics.monthlyRate')}</div>
<div class="stat-card-label">${t('statistics.studentCount')}</div>
<div class="stat-card-label">${t('statistics.classDays')}</div>
<div class="stat-card-label">${t('statistics.avgAbsent')}</div>

// 로딩
<div class="loading-state"><div class="spinner"></div><span>${t('statistics.calculating')}</span></div>

// 카드 제목
<div class="card-title">${t('statistics.dailyChart')}</div>
<div class="card-title">${t('statistics.studentTable')}</div>

// 테이블 헤더
<th ...>${t('statistics.colNumber')}</th>
<th ...>${t('statistics.colName')}</th>
<th ... style="color:var(--color-present);">${t('statistics.colPresent')}</th>
<th ... style="color:var(--color-absent);">${t('statistics.colAbsent')}</th>
<th ... style="color:var(--color-late);">${t('statistics.colLate')}</th>
<th ... style="color:var(--color-early);">${t('statistics.colEarly')}</th>
<th ...>${t('statistics.colRate')}</th>

// 데이터 없음
<tr><td colspan="7" ...>${t('statistics.noData')}</td></tr>
```

`_renderDailyChart()` 내 차트 데이터셋 라벨:
```js
{ label: t('status.present'), data: presentData, backgroundColor: 'rgba(16,185,129,0.7)', borderRadius: 3 },
{ label: t('status.absent'),  data: absentData,  backgroundColor: 'rgba(239,68,68,0.7)',  borderRadius: 3 },
```

- [ ] **Step 3: 커밋**

```bash
cd C:/test/attendance-app
git add js/pages/statistics.js
git commit -m "feat: i18n in statistics page"
```

---

## Task 12: `js/pages/calendar.js` + `js/pages/calendar-all.js` 업데이트

**Files:**
- Modify: `js/pages/calendar.js`
- Modify: `js/pages/calendar-all.js`

두 파일 모두 동일한 패턴:

- [ ] **Step 1: 두 파일 모두 import 수정**

```js
import { ..., formatYearMonth, ... } from '../utils/date.js';
import { t, getMessages } from '../utils/i18n.js';
```

`DOW_KO`는 DB 저장값 비교용이므로 변경 없음:
```js
const DOW_KO = ['일', '월', '화', '수', '목', '금', '토']; // 데이터용, locale 무관
```

달력 헤더(요일) 표시 시에는 `getMessages().date.daysShort[i]` 사용.

- [ ] **Step 2: calendar.js render() + mount() 한국어 교체**

`render()`:
```js
<h1 class="page-title">${escapeHtml(this.group.name)} — ${t('calendar.title')}</h1>
<span class="date-nav-label" id="month-label">${formatYearMonth(this.yearMonth)}</span>
<div class="empty-state-title">${t('common.groupNotFound')}</div>
```

`mount()` + `_changeMonth()`:
```js
if (labelEl) labelEl.textContent = formatYearMonth(this.yearMonth);
```

달력 컨테이너 로딩:
```js
<div class="loading-state"><div class="spinner"></div><span>${t('calendar.loading')}</span></div>
```

달력 헤더 렌더 (요일 표시) — 기존에 `DOW_KO[i]` 쓰는 부분을 표시용으로만 변경:
```js
// 달력 내 요일 헤더 렌더링 부분 찾아서:
const displayDow = getMessages().date.daysShort;
// DOW_KO[i] → displayDow[i]  (표시용)
// 단, s.attendanceDays.includes(DOW_KO[i]) 같은 데이터 비교는 DOW_KO 그대로 유지
```

- [ ] **Step 3: calendar-all.js render() + mount() 한국어 교체**

동일 패턴 적용:
```js
<h1 class="page-title">${t('calendar.allTitle')}</h1>
<span class="date-nav-label" id="month-label">${formatYearMonth(this.yearMonth)}</span>
<div class="loading-state"><div class="spinner"></div><span>${t('calendar.loading')}</span></div>
```

`STATUS_LABELS` import 교체:
```js
import { t } from '../utils/i18n.js';
// STATUS_LABELS[s] → t('status.' + s)
```

- [ ] **Step 4: 커밋**

```bash
cd C:/test/attendance-app
git add js/pages/calendar.js js/pages/calendar-all.js
git commit -m "feat: i18n in calendar pages"
```

---

## Task 13: `js/pages/settings.js` 업데이트 (언어 선택 UI 포함)

**Files:**
- Modify: `js/pages/settings.js`

**가장 중요한 태스크** — 모든 하드코딩 + 언어 선택 UI 추가.

- [ ] **Step 1: import 추가**

파일 상단에 추가:
```js
import { t, getLocale, setLocale, SUPPORTED_LOCALES } from '../utils/i18n.js';
import { put } from '../db/database.js'; // 이미 있음
```

- [ ] **Step 2: render() 전체 교체**

```js
async render() {
  const alertSettings   = await getByKey('settings', 'contract_alert') ?? {};
  const periodDays      = alertSettings.periodDays     ?? 7;
  const countRemaining  = alertSettings.countRemaining ?? 3;
  const savedLocale     = (await getByKey('settings', 'locale'))?.value ?? 'system';

  const user    = AuthService.currentUser();
  const isGuest = !user;
  this._hasLegacy = await hasLegacyData();

  const avatarUrl = user?.photoURL
    ? `<img src="${user.photoURL}" alt="${t('settings.account')}" style="width:48px;height:48px;border-radius:50%;object-fit:cover;">`
    : `<div style="width:48px;height:48px;border-radius:50%;background:var(--color-primary);display:flex;align-items:center;justify-content:center;color:white;font-size:20px;font-weight:700;">${(user?.displayName || '?')[0]}</div>`;

  const migrationSection = this._hasLegacy ? `
    <div class="card" style="margin-bottom: var(--space-4); border:1.5px solid var(--color-warning, #f59e0b);">
      <div class="card-header">
        <div class="card-title">${t('settings.migrationTitle')}</div>
      </div>
      <div class="card-body" style="display:flex; flex-direction:column; gap: var(--space-3);">
        <div style="font-size:13px; color:var(--color-text-muted);">${t('settings.migrationDesc')}</div>
        <button class="btn btn-primary" id="migrate-legacy-btn">${t('settings.migrateBtn')}</button>
      </div>
    </div>
  ` : '';

  const langOptions = [
    { value: 'system', label: t('settings.systemLanguage') },
    { value: 'ko',     label: '한국어' },
    { value: 'en',     label: 'English' },
  ];

  return `
    <div class="page-header">
      <div class="page-header-left">
        <h1 class="page-title">${t('settings.title')}</h1>
      </div>
    </div>
    <div class="page-body">

      <!-- 계정 -->
      <div class="card" style="margin-bottom: var(--space-4);">
        <div class="card-header">
          <div class="card-title">${t('settings.account')}</div>
        </div>
        <div class="card-body">
          ${isGuest ? `
            <div style="display:flex; align-items:center; gap:var(--space-3); margin-bottom:var(--space-4); padding:var(--space-3); background:var(--color-surface-2); border-radius:var(--radius-md);">
              <div style="font-size:28px;">👤</div>
              <div>
                <div style="font-weight:700; font-size:15px;">${t('settings.localMode')}</div>
                <div style="font-size:13px; color:var(--color-text-muted);">${t('settings.localModeDesc')}</div>
              </div>
            </div>
            <div style="font-size:13px; color:var(--color-text-muted); margin-bottom:var(--space-3);">${t('settings.loginGoogleDesc')}</div>
            <button class="btn btn-primary" id="login-btn" style="width:100%;">${t('settings.loginGoogle')}</button>
          ` : `
            <div style="display:flex; align-items:center; gap:var(--space-4); margin-bottom:var(--space-4);">
              ${avatarUrl}
              <div style="flex:1; min-width:0;">
                <div style="font-weight:700; font-size:15px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap;">${user?.displayName || t('settings.noName')}</div>
                <div style="font-size:13px; color:var(--color-text-muted); overflow:hidden; text-overflow:ellipsis; white-space:nowrap;">${user?.email || ''}</div>
              </div>
            </div>
            <button class="btn btn-secondary" id="logout-btn" style="width:100%;">${t('settings.logout')}</button>
          `}
        </div>
      </div>

      <!-- 언어 -->
      <div class="card" style="margin-bottom: var(--space-4);">
        <div class="card-header">
          <div class="card-title">${t('settings.language')}</div>
        </div>
        <div class="card-body" style="display:flex; flex-direction:column; gap: var(--space-3);">
          ${langOptions.map(opt => `
            <label style="display:flex; align-items:center; gap:var(--space-3); cursor:pointer; padding: var(--space-2) 0;">
              <input type="radio" name="locale" value="${opt.value}"
                ${savedLocale === opt.value ? 'checked' : ''}
                style="width:16px;height:16px;accent-color:var(--color-primary);">
              <span style="font-size:15px; font-weight:${savedLocale === opt.value ? '600' : '400'};">${opt.label}</span>
            </label>
          `).join('')}
        </div>
      </div>

      ${migrationSection}

      <!-- 계약 종료 알림 -->
      <div class="card" style="margin-bottom: var(--space-4);">
        <div class="card-header">
          <div class="card-title">${t('settings.contractAlert')}</div>
        </div>
        <div class="card-body" style="display:flex; flex-direction:column; gap: var(--space-4);">
          <div style="display:flex; align-items:center; justify-content:space-between; gap:var(--space-4);">
            <div>
              <div style="font-weight:600; margin-bottom:2px;">${t('settings.periodType')}</div>
              <div style="font-size:13px; color:var(--color-text-muted);">${t('settings.periodTypeDesc')}</div>
            </div>
            <div style="display:flex; align-items:center; gap:6px; flex-shrink:0;">
              <input type="number" id="alert-period-days" class="form-input"
                value="${periodDays}" min="1" max="365"
                style="width:70px; text-align:center; padding:6px 8px;">
              <span style="font-size:14px; color:var(--color-text-muted);">${t('settings.daysUnit')}</span>
            </div>
          </div>
          <div class="divider"></div>
          <div style="display:flex; align-items:center; justify-content:space-between; gap:var(--space-4);">
            <div>
              <div style="font-weight:600; margin-bottom:2px;">${t('settings.countType')}</div>
              <div style="font-size:13px; color:var(--color-text-muted);">${t('settings.countTypeDesc')}</div>
            </div>
            <div style="display:flex; align-items:center; gap:6px; flex-shrink:0;">
              <input type="number" id="alert-count-remaining" class="form-input"
                value="${countRemaining}" min="0" max="999"
                style="width:70px; text-align:center; padding:6px 8px;">
              <span style="font-size:14px; color:var(--color-text-muted);">${t('settings.countUnit')}</span>
            </div>
          </div>
          <div>
            <button class="btn btn-primary" id="save-alert-settings-btn">${t('settings.saveBtn')}</button>
          </div>
        </div>
      </div>

      <!-- 앱 정보 -->
      <div class="card">
        <div class="card-header"><div class="card-title">${t('settings.appInfo')}</div></div>
        <div class="card-body" style="display:flex; flex-direction:column; gap:var(--space-2);">
          <div style="display:flex; justify-content:space-between; font-size:14px;">
            <span style="color:var(--color-text-muted);">${t('settings.version')}</span>
            <span style="font-weight:600;">1.0.0</span>
          </div>
          <div style="display:flex; justify-content:space-between; font-size:14px;">
            <span style="color:var(--color-text-muted);">${t('settings.developer')}</span>
            <span style="font-weight:600;">ISTECH</span>
          </div>
        </div>
      </div>

    </div>
  `;
}
```

- [ ] **Step 3: mount() 에 언어 선택 이벤트 추가**

`mount()` 내:
```js
// 언어 선택 라디오
document.querySelectorAll('input[name="locale"]').forEach(radio => {
  radio.addEventListener('change', async (e) => {
    await setLocale(e.target.value);
    // localechange 이벤트가 앱 전체 재렌더를 트리거함
    // settings 페이지 자체도 재렌더되므로 추가 작업 불필요
  });
});
```

모든 Toast/Modal 문자열 교체:
```js
// _loginFromGuest():
Toast.error(t('messages.loginFailed', { msg: e.message || '' }));

// _logout():
Modal.confirm({ title: t('settings.logout'), message: t('settings.logoutConfirm'), confirmText: t('settings.logout') });
Toast.error(t('messages.logoutFailed', { msg: e.message }));

// _migrateLegacy():
Modal.confirm({ title: t('settings.migrateModalTitle'), message: t('settings.migrateModalMsg'), danger: true, confirmText: t('settings.migrateModalBtn') });
btn.textContent = t('settings.migratingBtn');
Toast.success(t('messages.migrateSuccess'));
Toast.error(t('messages.migrateFailed', { msg: e.message }));
btn.textContent = t('settings.migrateBtn');

// _saveAlertSettings():
Toast.error(t('messages.periodAlertError'));
Toast.error(t('messages.countAlertError'));
Toast.success(t('messages.alertSaved'));
```

- [ ] **Step 4: 커밋**

```bash
cd C:/test/attendance-app
git add js/pages/settings.js
git commit -m "feat: i18n in settings page + language selector UI"
```

---

## Task 14: `js/pages/export.js` + `js/pages/premium.js` + `index.html` 업데이트

**Files:**
- Modify: `js/pages/export.js`
- Modify: `js/pages/premium.js`
- Modify: `index.html`

### export.js

- [ ] **Step 1: export.js import 교체 + 한국어 교체**

```js
import { t } from '../utils/i18n.js';
// MESSAGES import 제거
```

`render()`:
```js
<h1 class="page-title">${escapeHtml(this.group.name)} — ${t('export.title')}</h1>
<div class="empty-state-title">${t('common.groupNotFound')}</div>
```

`_exportForm()`:
```js
<div class="card-title">${t('export.dateRange')}</div>
<label class="form-label" for="start-date">${t('export.startDate')}</label>
<label class="form-label" for="end-date">${t('export.endDate')}</label>
<button class="btn btn-primary" id="export-csv-btn">${t('export.csvBtn')}</button>
<button class="btn btn-secondary" id="export-pdf-btn">${t('export.pdfBtn')}</button>
```

Toast 메시지:
```js
Toast.success(t('messages.exportDone'));
Toast.error(t('messages.saveFailed'));
```

### premium.js

- [ ] **Step 2: premium.js import 교체 + 한국어 교체**

기존:
```js
import { MESSAGES, PREMIUM_PRICE } from '../utils/i18n.js';
```
교체:
```js
import { PREMIUM_PRICE, t } from '../utils/i18n.js';
```

이미 프리미엄인 경우:
```js
<h1 class="page-title">${t('premium.title')}</h1>
<div class="empty-state-title">${t('premium.alreadyTitle')}</div>
<div class="empty-state-desc">${t('premium.alreadyDesc')}</div>
<a href="#/" class="btn btn-primary" style="margin-top:12px;">${t('premium.goHome')}</a>
```

업그레이드 페이지:
```js
<h1 class="page-title">${t('premium.upgradeTitle')}</h1>
// 히어로 섹션:
<div ...>${t('premium.heroTitle')}</div>
<div ...>${t('premium.heroDesc')}</div>
<div ...>${PREMIUM_PRICE}</div>
<div ...>${t('premium.lifetime')}</div>

// 기능 목록 (f1~f4 키 사용):
${[1,2,3,4].map(i => `
  <div class="feature-item">
    <div style="font-size:28px;">${t('premium.f' + i + 'Icon')}</div>
    <div>
      <div style="font-weight:700;">${t('premium.f' + i + 'Title')}</div>
      <div style="font-size:13px; color:var(--color-text-muted);">${t('premium.f' + i + 'Desc')}</div>
    </div>
  </div>
`).join('')}
```

Toast 교체:
```js
Toast.success(t('messages.premiumActivated'));
```

### index.html

- [ ] **Step 3: index.html 수정**

`<html lang="ko">` → `<html lang="ko">` 유지하되 앱 초기화 후 JS에서 동적으로 변경 (app.js의 localechange 리스너에 추가):

```js
// app.js의 localechange 리스너에 추가:
window.addEventListener('localechange', (e) => {
  document.documentElement.lang = e.detail?.locale || getLocale();
  Sidebar.render();
  BottomNav.render();
  _router.refresh();
});
```

`initLocale()` 호출 직후 초기값도 설정:
```js
await initLocale();
document.documentElement.lang = getLocale();
```

메타 태그 설명 수정 (영어 앱에서도 동작하도록 중립적으로):
```html
<meta name="description" content="Attendance — manage student attendance easily">
```

- [ ] **Step 4: 커밋**

```bash
cd C:/test/attendance-app
git add js/pages/export.js js/pages/premium.js index.html js/app.js
git commit -m "feat: i18n in export/premium pages + html lang attr"
```

---

## Task 15: Android 빌드 + Sync

**Files:**
- `android/` (자동 업데이트됨)

- [ ] **Step 1: 빌드 및 Android sync**

```bash
cd C:/test/attendance-app
node build.js && npx cap sync android
```

Expected: `✔ copy android` + `✔ update android` 메시지

- [ ] **Step 2: android 변경사항 커밋**

```bash
cd C:/test/attendance-app
git add android/
git commit -m "chore: sync android after i18n implementation"
```

---

## 검증 체크리스트

구현 완료 후 다음을 확인:

- [ ] 기기 언어 영어 → 앱 전체가 영어로 표시
- [ ] 기기 언어 한국어 → 앱 전체가 한국어로 표시
- [ ] 설정 > 언어 > English 선택 → 즉시 전환 (저장 버튼 없음)
- [ ] 설정 > 언어 > 시스템 언어 사용 → 기기 언어로 복귀
- [ ] 날짜 형식: 영어로 "March 15, 2024 (Fri)", 한국어로 "2024년 3월 15일 (금)"
- [ ] 출석 버튼 텍스트: 영어 "Present/Absent/Late/Early Leave"
- [ ] 달력 요일 헤더: 영어 "Sun/Mon/Tue...", 한국어 "일/월/화..."
- [ ] 대시보드 계약 만료: "5 days left" / "5일 남음"
- [ ] 학생 추가/삭제 Toast 메시지 번역됨
- [ ] DB 저장된 요일 데이터(월/화/수...)는 변경 없이 그대로 동작
